# Shared iOS configuration
# IOS_BUNDLE_ID: Complete app identifier from ENV

bundle_id = ENV['IOS_BUNDLE_ID']
UI.user_error!("❌ IOS_BUNDLE_ID is required!") unless bundle_id

app_identifier(bundle_id)
